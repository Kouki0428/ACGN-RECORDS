# ACGN Records 技术方案设计文档

> 版本：v0.1（初步设计）
> 目标平台：Windows（Electron 桌面应用）
> 定位：轻量级 ACGN 进度追踪与统计工具，本地优先、可选同步到 Bangumi
> 说明：本文为技术方案设计，不含应用实现代码。

---

## 0. 目标与范围

**核心目标**
- 一站管理动画 / 轻小说 / 漫画 / Galgame / 单机游戏的观看 / 阅读 / 游玩进度。
- 本地 SQLite 存储用户进度、自定义标签、购买信息。
- 进度可单向 / 双向同步到 Bangumi（TMDB、VNDB 作为检索与元数据的补充源）。
- **明确不做**：本地媒体播放、文件管理、BT/网盘下载。

**非目标（避免范围蔓延）**
- 不做社区 / 社交 / 论坛功能。
- 不做媒体解码与播放。
- 不做跨设备云同步（除 Bangumi 官方同步外）。

---

## 1. 项目整体架构设计

### 1.1 技术栈总览

| 层 | 选型 | 说明 |
|---|---|---|
| 桌面框架 | Electron（最新稳定版） | 跨平台壳，Windows 优先 |
| 前端框架 | Vue 3（Composition API） | 配合 `<script setup>` |
| 语言 | TypeScript（推荐） | 类型安全，降低 IPC/数据库出错率 |
| 构建 | Vite + `vite-plugin-electron` | 热更新 + 产物打包 |
| 状态管理 | Pinia | 按模块切分 store |
| 路由 | Vue Router | 左侧分类导航对应路由 |
| 本地数据库 | `better-sqlite3` | 同步 API、零异步回调、性能优异 |
| 加密存储 | Electron 内置 `safeStorage` | 加密 Bangumi token，落盘不裸存 |
| 打包 | `electron-builder` | NSIS 安装包、自动更新 |
| HTTP 客户端 | 原生 `fetch`（主进程） | 统一拦截器做限流 / 重试 |

### 1.2 进程模型（Main / Preload / Renderer）

```
┌─────────────────────────────────────────────┐
│  Renderer (Vue 3)                            │
│  views / components / pinia stores           │
│        │ 调用 window.acgn.* (contextBridge)  │
│        ▼                                     │
│  Preload (contextBridge, 仅暴露安全 API)     │
│        │  ipcRenderer.invoke(channel, args) │
└────────┼────────────────────────────────────┘
         │ IPC（contextIsolated = true）
┌────────┼────────────────────────────────────┐
│  Main Process                               │
│  • 窗口管理 / 应用生命周期 / 自动更新        │
│  • 个人令牌存储（safeStorage 加密）          │
│  • API 适配层（Bangumi / TMDB / VNDB）       │
│  • Sync Engine（双向同步 + 冲突处理）        │
│  • DB Service（better-sqlite3 封装）         │
│  • 加密存储（safeStorage）                   │
└─────────────────────────────────────────────┘
```

**关键安全约束**
- `contextIsolation = true`，`nodeIntegration = false`。
- Preload 只暴露白名单方法（如 `window.acgn.db.query`、`window.acgn.api.search`），绝不直接暴露 `require` / `fs`。
- 所有网络请求、token、密钥只在 **主进程** 内处理，renderer 拿不到明文 token。
- API 密钥 / token 落盘前用 `safeStorage.encryptString` 加密，读取时 `decryptString`。

### 1.3 离线优先（Offline-First）

- 所有检索结果、作品详情、剧集列表写进本地 `subjects` / `episodes` 表（带 `raw_json` 缓存原文）。
- 浏览、统计、"点格子"等日常操作**完全离线可用**。
- 仅在用户点击"同步"或自动同步开关开启时，才与主进程 API 层通信。
- 本地修改标记 `dirty = 1`，由 Sync Engine 统一回传 Bangumi。

### 1.4 数据流与同步模型

```
本地 SQLite  ◀── pull(可选) ──┐
     ▲                         │
     │ dirty 标记              ▼
  用户操作 ──► Sync Engine ──► Bangumi API
```

- **推（push）**：本地 `collections` / `episode_progress` 变化 → 调用 Bangumi 收藏接口。
- **拉（pull）**：首次绑定账号或手动刷新 → 拉取 Bangumi 收藏，合并进本地（仅当本地无冲突时覆盖）。
- **冲突策略**：默认"时间戳后写覆盖"（last-write-wins）；对 ep_status 等计数型字段，pull 时取 `max(本地, 远端)`，避免互相覆盖丢失进度。

### 1.5 隐私与数据安全

- 用户数据全部位于 `app.getPath('userData')/acgn-records.db`。
- token 加密存储；除用户主动发起的 Bangumi 同步外，不向任何第三方服务器发送数据。
- TMDB / VNDB 仅用于检索 / 补充元数据，不发送用户进度。
- 提供"清除本地数据 / 退出登录"按钮，一键 `DELETE FROM` + 清空 token。

---

## 2. 建议的目录结构

```
acgn-records/
├── electron/                      # 主进程代码（TypeScript）
│   ├── main.ts                    # 入口：窗口 / 生命周期 / 自动更新
│   ├── preload.ts                 # contextBridge 安全桥
│   ├── ipc/                       # IPC 通道注册
│   │   ├── db.ipc.ts
│   │   ├── api.ipc.ts
│   │   ├── auth.ipc.ts
│   │   └── sync.ipc.ts
│   ├── services/                  # 主进程服务
│   │   ├── db/
│   │   │   ├── connection.ts      # better-sqlite3 单例
│   │   │   ├── migrations/        # 版本化迁移脚本
│   │   │   └── repositories/      # 各表 CRUD 封装
│   │   ├── api/
│   │   │   ├── bangumi.ts         # Bangumi 适配
│   │   │   ├── tmdb.ts            # TMDB 适配
│   │   │   ├── vndb.ts            # VNDB 适配
│   │   │   └── normalizer.ts      # 统一 Subject 模型
│   │   ├── auth/
│   │   │   └── oauth.ts           # 个人令牌存储 + token 管理
│   │   ├── sync/
│   │   │   └── syncEngine.ts      # 双向同步 + 冲突
│   │   └── crypto/
│   │       └── vault.ts           # safeStorage 加解密
│   └── menu.ts                    # 原生菜单（可选）
│
├── src/                           # Renderer（Vue 3）
│   ├── main.ts
│   ├── App.vue
│   ├── router/
│   │   └── index.ts               # /anime /light-novel /manga /galgame /game
│   ├── stores/                    # Pinia
│   │   ├── auth.ts
│   │   ├── collections.ts
│   │   ├── anime.ts
│   │   ├── statistics.ts
│   │   └── settings.ts
│   ├── services/                  # 渲染层封装（仅调 window.acgn.*）
│   │   ├── apiClient.ts
│   │   └── dbClient.ts
│   ├── views/                     # 模块页面
│   │   ├── AnimeView.vue
│   │   ├── LightNovelView.vue
│   │   ├── MangaView.vue
│   │   ├── GalgameView.vue
│   │   ├── GameView.vue
│   │   └── SettingsView.vue
│   ├── components/
│   │   ├── SidebarNav.vue
│   │   ├── SubjectCard.vue
│   │   ├── SearchBar.vue
│   │   ├── EpisodeGrid.vue        # 「点格子」组件
│   │   ├── EpisodeComments.vue    # 单集评论面板
│   │   ├── ProgressEditor.vue
│   │   ├── PurchaseInfo.vue       # 购买平台 / 价格
│   │   ├── TagEditor.vue
│   │   └── StatCards.vue
│   ├── composables/               # 复用逻辑（useSync 等）
│   └── assets/
│
├── resources/                     # 图标 / 安装包素材
├── build/                         # electron-builder 配置
├── scripts/                       # 构建 / 迁移辅助
├── package.json
├── vite.config.ts
├── tsconfig.json
└── README.md
```

**要点**
- `electron/services` 是唯一能碰网络、DB、密钥的地方；`src/services` 只是 IPC 封装，不直接 import 主进程模块。
- 每个模块（动画 / 轻小说 / 漫画 / Galgame / 游戏）在 `views/` + `stores/` 成对出现，降低耦合。

---

## 3. SQLite 数据库初步表结构设计

### 3.1 设计原则
- **缓存与用户数据分离**：`subjects` / `episodes` 缓存 API 数据；`collections` / `episode_progress` / `purchases` 为用户私有数据。
- **统一作品表 + 类别字段**：五类作品共用 `subjects`，用 `category` 区分，避免五张结构相似的表。
- **保留 raw_json**：API 字段演进时无需立即改表，前端可临时取原文。

### 3.2 表结构（初始 DDL）

```sql
-- 账号 / 授权（token 字段为 safeStorage 加密后的密文）
CREATE TABLE accounts (
  id            INTEGER PRIMARY KEY AUTOINCREMENT,
  provider      TEXT    NOT NULL DEFAULT 'bangumi',
  username      TEXT,
  user_id       INTEGER,
  access_token  TEXT,                 -- 加密存储
  refresh_token TEXT,                 -- 加密存储
  expires_at    INTEGER,              -- 过期时间戳
  created_at    INTEGER NOT NULL DEFAULT (strftime('%s','now')),
  updated_at    INTEGER NOT NULL DEFAULT (strftime('%s','now'))
);

-- 作品缓存（来自 Bangumi / TMDB / VNDB）
CREATE TABLE subjects (
  id                 INTEGER PRIMARY KEY AUTOINCREMENT,
  provider           TEXT    NOT NULL,      -- bangumi | tmdb | vndb
  provider_subject_id TEXT   NOT NULL,
  category           TEXT    NOT NULL,      -- anime | light_novel | manga | galgame | game
  title              TEXT,
  title_cn           TEXT,
  summary            TEXT,
  image_url          TEXT,
  air_date           TEXT,                  -- 首播/发售 YYYY-MM-DD
  end_date           TEXT,
  total_episodes     INTEGER,              -- 动画总集数
  total_volumes      INTEGER,              -- 漫画/轻小说卷数
  rating             REAL,
  raw_json           TEXT,                 -- 原始 API 响应
  created_at         INTEGER NOT NULL DEFAULT (strftime('%s','now')),
  updated_at         INTEGER NOT NULL DEFAULT (strftime('%s','now')),
  UNIQUE(provider, provider_subject_id)
);
CREATE INDEX idx_subjects_category ON subjects(category);

-- 用户收藏 / 进度（每个账号 + 作品一行）
CREATE TABLE collections (
  id               INTEGER PRIMARY KEY AUTOINCREMENT,
  account_id       INTEGER NOT NULL,
  subject_id       INTEGER NOT NULL,
  status           INTEGER NOT NULL DEFAULT 3,  -- 1想 2完 3在 4搁置 5弃
  ep_status        INTEGER DEFAULT 0,           -- 已看集数(动画)
  vol_status       INTEGER DEFAULT 0,           -- 已读卷数(轻小说/漫画)
  route_status     INTEGER DEFAULT 0,           -- 已通关路线数(Galgame)
  rating           INTEGER,                     -- 用户评分 1-10
  comment          TEXT,
  private          INTEGER DEFAULT 0,
  last_sync_at     INTEGER,                     -- 最近同步时间
  local_updated_at INTEGER NOT NULL DEFAULT (strftime('%s','now')),
  dirty            INTEGER NOT NULL DEFAULT 0,  -- 是否有未同步修改
  UNIQUE(account_id, subject_id),
  FOREIGN KEY (account_id) REFERENCES accounts(id),
  FOREIGN KEY (subject_id) REFERENCES subjects(id)
);
CREATE INDEX idx_collections_status ON collections(status);

-- 剧集列表（动画）
CREATE TABLE episodes (
  id                 INTEGER PRIMARY KEY AUTOINCREMENT,
  subject_id         INTEGER NOT NULL,
  provider_episode_id TEXT,
  ep_number          INTEGER,
  ep_type            INTEGER DEFAULT 0,   -- 0本篇 1SP 2OP 3ED ...
  title              TEXT,
  airdate            TEXT,
  comment_count      INTEGER DEFAULT 0,
  sort               INTEGER,
  raw_json           TEXT,
  UNIQUE(subject_id, provider_episode_id),
  FOREIGN KEY (subject_id) REFERENCES subjects(id)
);
CREATE INDEX idx_episodes_subject ON episodes(subject_id);

-- 「点格子」逐集状态
CREATE TABLE episode_progress (
  id           INTEGER PRIMARY KEY AUTOINCREMENT,
  collection_id INTEGER NOT NULL,
  episode_id    INTEGER NOT NULL,
  watched       INTEGER NOT NULL DEFAULT 0,
  watched_at    INTEGER,
  FOREIGN KEY (collection_id) REFERENCES collections(id),
  FOREIGN KEY (episode_id) REFERENCES episodes(id),
  UNIQUE(collection_id, episode_id)
);

-- 自定义标签
CREATE TABLE tags (
  id    INTEGER PRIMARY KEY AUTOINCREMENT,
  name  TEXT NOT NULL UNIQUE,
  color TEXT
);
CREATE TABLE collection_tags (
  collection_id INTEGER NOT NULL,
  tag_id        INTEGER NOT NULL,
  PRIMARY KEY (collection_id, tag_id),
  FOREIGN KEY (collection_id) REFERENCES collections(id),
  FOREIGN KEY (tag_id) REFERENCES tags(id)
);

-- 购买信息（Galgame / 游戏）
CREATE TABLE purchases (
  id           INTEGER PRIMARY KEY AUTOINCREMENT,
  collection_id INTEGER NOT NULL,
  platform      TEXT,                  -- Steam / GOG / DMM / Johren / 官网 / 实体 ...
  price         REAL,
  currency      TEXT DEFAULT 'CNY',
  purchased_at  INTEGER,
  note          TEXT,
  FOREIGN KEY (collection_id) REFERENCES collections(id)
);
CREATE INDEX idx_purchases_collection ON purchases(collection_id);

-- 同步日志（排错 / 审计）
CREATE TABLE sync_log (
  id         INTEGER PRIMARY KEY AUTOINCREMENT,
  account_id INTEGER NOT NULL,
  direction  TEXT,                     -- push | pull
  entity     TEXT,                     -- subject | collection | episode
  entity_id  INTEGER,
  status     TEXT,                     -- success | failed | conflict
  message    TEXT,
  created_at INTEGER NOT NULL DEFAULT (strftime('%s','now'))
);

-- 应用设置（key-value）
CREATE TABLE settings (
  key   TEXT PRIMARY KEY,
  value TEXT
);
```

### 3.3 关键说明
- `collections.dirty` 是同步核心：任何本地进度变更后置 1，Sync Engine 推送成功后清零。
- `episode_progress.watched` 是"点格子"的真实状态；`collections.ep_status` 冗余存储 `COUNT(watched=1)`，用于快速统计与 Bangumi 上传。
- 轻小说 / 漫画用 `vol_status`（卷）记录进度；漫画也可扩展 `chapter_status` 记录"话"，初期可用 `vol_status` + 备注，后期按需加 `chapter_progress` 表。
- `purchases` 与 `collections` 一对多，支持同一作品多平台购买（如实体 + 数字版）。

---

## 4. 与 Bangumi / TMDB / VNDB 的 API 集成方案

### 4.1 统一适配层

```
API 请求（renderer 触发）
   → ipc api.search({keyword, category})
      → api/ipc 路由到对应 provider 适配器
         → 适配器调用官方 API（带限流/重试）
            → normalizer 转成统一 Subject 模型
               → 写缓存(subjects) → 返回 renderer
```

**统一 `Subject` 模型（normalizer 输出）**
```ts
interface Subject {
  provider: 'bangumi' | 'tmdb' | 'vndb';
  providerSubjectId: string;
  category: 'anime' | 'light_novel' | 'manga' | 'galgame' | 'game';
  title: string;
  titleCn?: string;
  summary?: string;
  imageUrl?: string;
  airDate?: string;
  totalEpisodes?: number;
  totalVolumes?: number;
  rating?: number;
}
```

### 4.2 Bangumi 集成（主数据源 + 同步目标）

**鉴权（个人访问令牌 Personal Access Token 模式）**

> Bangumi 新公开 API 同时支持 OAuth 与个人令牌两种鉴权。本应用是**个人本地工具**，采用更简单的个人令牌模式——无需在后台注册应用、无需 `client_id`/`client_secret`、无需本地回调服务器、无需弹浏览器授权窗口。

1. 用户访问 https://next.bgm.tv/demo/access-token 一键生成 Access Token，自行复制到剪贴板。
2. 在应用「设置」页粘贴 Token 并保存；主进程用 `safeStorage` 加密后 **UPSERT** 进 `accounts` 表（同一 provider 只保留一行）。
3. 保存时主进程立即用该 Token 调用 `GET /v0/me` 拉取用户名/昵称并缓存到 `accounts`（仅用于展示，失败不影响保存）。
4. 个人令牌**长期有效、无 refresh_token**；若用户在 Bangumi 侧重置/失效令牌，在设置页重新粘贴即可覆盖。
5. 退出登录 = 清除 `accounts` 中该行 + 清空内存内的 token 缓存。

**检索与详情**
- 搜索：`GET https://api.bgm.tv/v0/search/subject/{kw}?type={subjectType}&limit=20`
  - subjectType 映射：`anime→2`，`light_novel/manga→1`（book），`galgame/game→4`。
  - 轻小说 / 漫画同属 type=1，靠 `infobox` 或标签（漫画/小说）二次区分，或在搜索词中追加"漫画""轻小说"提示。
- 详情：`GET /v0/subjects/{id}?fields=...`（含评分、排名、标签）。
- 剧集：`GET /v0/episodes?subject_id={id}&type=0&limit=100&offset=0`（本篇剧集 + 评论数）。
- 单集评论：`GET /v0/episodes/{episodeId}/comments?limit=20&offset=0`（点击格子时拉取并展示）。
- 本季新番：`GET /v0/calendar`（用于"本季在看"统计与首页推荐）。

**进度同步**
- 更新收藏：`POST /v0/users/-/collections/{subjectId}`
  请求体：`{ type, rate?, comment?, tags?, ep_status?, vol_status?, private? }`
  - `type`：1想看/读/玩，2看过/读完/通关，3在看/在读/在玩，4搁置，5抛弃。
  - 动画上传 `ep_status` = 已看集数；轻小说 / 漫画上传 `vol_status`；Galgame / 游戏上传 `type=2`（通关）或 `type=3`（在玩）。
- 拉取我的收藏：`GET /v0/users/{username}/collections?subject_id={id}`（合并进本地）。
- 请求头：`Authorization: Bearer {token}` + 自定义 `User-Agent`（Bangumi 强制要求）。

### 4.3 TMDB 集成（动画元数据补充）

- 鉴权：API Key（v3）或 Bearer v4 token，存于 `settings`（加密）。
- 用途：补充 / 校正动画的海报、简介、单集标题与播出日期；在 Bangumi 缺图或缺英文标题时兜底。
- 主要端点：
  - 搜索：`GET https://api.themoviedb.org/3/search/tv?query={kw}&language=zh-CN`
  - 详情：`GET /3/tv/{id}`
  - 单集：`GET /3/tv/{id}/season/{s}/episode/{e}`（拿 episode 级简介，可选展示）。
- 限流：约 50 次/秒，做好客户端并发控制即可。

### 4.4 VNDB 集成（Galgame 主检索源）

- 协议：POST `https://api.vndb.org/kana`（Kana API，GraphQL 风格查询语言）。
- 鉴权：可选 token；未鉴权有较严格限流，建议申请 token 并用主进程保管。
- 用途：Galgame 的权威元数据源（标题、别名、简介、发售日、标签、制作商）。
- 示例查询：
  ```
  query {
    vn {
      id title alttitle
      image { url }
      released
      description
      tags { name }
    }
  }
  ```
- 注意：VNDB 主要提供元数据，**不负责用户进度同步**；Galgame 进度仍按需求上传到 Bangumi（subjectType=4）。

### 4.5 限流 / 重试 / 缓存
- 主进程维护每 provider 的令牌桶（Bangumi 尤其注意，429 时退避重试）。
- 所有检索结果写 `subjects` 缓存，TTL 可配置（默认 7 天），过期才回源。
- 失败统一进 `sync_log` / 错误提示，不阻塞 UI。

### 4.6 双向同步引擎（Sync Engine）

```
触发：手动「同步」按钮 / 启动时自动 / 定时
1. 收集 collections WHERE dirty=1 → push 到 Bangumi（逐条）
2. （可选）pull 我的 Bangumi 收藏 → 与本地 merge
3. 冲突规则：
   - 计数型(ep_status/vol_status/route_status)：取 max(本地, 远端)
   - 状态型(status)：以 local_updated_at 较新者为准
   - 发生 conflict 写入 sync_log，必要时弹窗让用户确认
4. 成功后清 dirty、写 last_sync_at
```

---

## 5. 核心功能模块拆分与职责划分

### 5.1 通用 / 基础设施模块
| 模块 | 职责 |
|---|---|
| `auth` store + `oauth` service | Bangumi 个人令牌保存/清除、退出登录 |
| `dbClient` / `db` ipc | 所有本地读写的安全入口 |
| `apiClient` + 三个适配器 | 统一检索、normalizer、缓存 |
| `syncEngine` | 双向同步、冲突、日志 |
| `statistics` service | 跨模块统计计算 |
| `settings` store | 主题、自动同步开关、API Key |

### 5.2 动画模块（AnimeView）
- 搜索动画 → 加入"在看"（Bangumi `type=3`）。
- **点格子**：`EpisodeGrid` 渲染 `episodes`，点击切换 `episode_progress.watched`，自动重算 `collections.ep_status` 并标记 `dirty`。
- 点击单集 → `EpisodeComments` 拉取 Bangumi 该集评论区并展示。
- 进度变化时经 Sync Engine 上传 `ep_status`。

### 5.3 轻小说模块（LightNovelView）
- 搜索（Bangumi type=1 + 标签过滤"轻小说"）。
- 记录"第 X 卷" → `collections.vol_status`。
- 上传 `vol_status` 到 Bangumi；支持自定义标签。

### 5.4 漫画模块（MangaView）
- 搜索（Bangumi type=1 + "漫画"）。
- 记录"第 X 话 / 第 X 卷" → 初期用 `vol_status` + 备注，后续可加 `chapter_progress` 表。
- 上传 `vol_status`；自定义标签。

### 5.5 Galgame 模块（GalgameView）
- 检索以 **VNDB** 为主，Bangumi（type=4）为辅。
- 记录"已通关 X 条路线" → `collections.route_status`。
- **购买信息**：`PurchaseInfo` 写 `purchases`（平台 + 价格）。
- 进度上传 Bangumi（`type=2` 通关 / `type=3` 在玩）。

### 5.6 单机游戏模块（GameView）
- 搜索（Bangumi type=4，或 TMDB 无游戏 → 主要靠 Bangumi）。
- 记录状态：想玩 / 在玩 / 已通关 / 搁置 / 抛弃（对应 `status`）。
- **购买信息**：同 Galgame 写 `purchases`。
- 上传 `status` 到 Bangumi。

### 5.7 统计模块（StatCards / statistics）
- "本季在看 X 部"：按当前季度（冬 1-3 / 春 4-6 / 夏 7-9 / 秋 10-12 月）过滤 `subjects.air_date` + `collections.status=3` + `category=anime`。
- "总计观看 X 集"：`COUNT(episode_progress.watched=1)`。
- "本年读完 X 卷""已通关 X 部游戏""花费合计 ¥X"（来自 `purchases`）等可扩展卡片。

---

## 6. 关键技术点与后续步骤

**风险 / 注意**
1. Bangumi 要求自定义 `User-Agent`，否则 403。
2. Bangumi 轻小说与漫画同为 type=1，检索时需用关键词 / 标签二次区分。
3. VNDB / TMDB 限流需在主进程统一管控，避免触发封禁。
4. `better-sqlite3` 是原生模块，打包时需 `electron-builder` 正确处理二进制（或用 `electron-rebuild`）。

**实现现状（截至 v0.2）**
- 技术栈：已采用 TypeScript + Vue 3 + Vite + Pinia + Electron + `better-sqlite3` + `safeStorage`，三端（main/preload/renderer）经 `window.acgn` contextBridge 打通，`npm run build` 编译通过（含 vue-tsc）。
- 离线优先：本地 SQLite 为权威缓存，仅用户主动同步才联网；检索结果 / 作品详情 / 剧集 / CG 画廊均落盘，断网可读。
- 同步策略：已实现「手动（上传 / 拉取 / 双向同步）」+「自动同步开关（默认关，开启后每 5 分钟 pushAll）」双模式；pullAll 为整库导入（本地 = 离线镜像）。
- Bangumi 鉴权：**双模式并存** —— ① 个人令牌（next.bgm.tv/demo/access-token 生成粘贴）；② OAuth 授权码一键登录（应用凭证硬编码进应用，回调固定 `http://localhost:7321/oauth/callback`，**用户须在 Bangumi 应用后台登记该回调**）。两者均 Bearer 头、safeStorage 加密落盘。
- 分类（4 类，已删除"单机游戏"栏）：`anime` / `light_novel` / `manga` / `galgame`。Bangumi 书籍(type=1)按 API `platform` 字段优先细分为轻小说 / 漫画（`platform`='小说'→轻小说，'漫画'→漫画；缺失时按 **tag 加权计分**兜底）；游戏(type=4)统一归 Galgame。
- 详情页：动画 / 轻小说 / 漫画 / Galgame 四个详情均通过 `SubjectMetaPanel` 展示 **tags 与制作信息（infobox）**，首次打开联网补全并缓存到 `subjects.tags` / `subjects.infobox`，离线可读。
- Galgame CG 画廊（游戏画廊）：复刻 Bangumi 超合金组件「游戏画廊」(app/6179) 逻辑 —— 从 Bangumi infobox 解析 **VNDB / DLsite / Steam** 三类外链，取**真实游戏内 CG / 截图**（VNDB `screenshots` 含 sexual/violence 分级、DLsite 直链探测样例 CG、Steam 走 worker 代理 + appdetails 兜底）；UI 为 VNDB/DLsite/Steam 标签页 + 横向缩略图条 + R18 模糊开关 + 点击灯箱（上一张/下一张/ESC/方向键/计数）。**画廊只保留真实 CG，不含封面**；缓存带版本号（v2），旧版含封面的缓存自动失效。
- 已全部落地：脚手架 + DB 层 + 四大模块 MVP（动画点格子 / 轻小说 / 漫画 / Galgame）+ Bangumi 双向同步引擎 + 购买信息记录 + 详情 tags/制作信息 + CG 画廊。

**后续可选增强（非阻塞）**
- 动画真实剧集接入（当前用占位剧集，`bangumi.getEpisodes` 已就绪）。
- 漫画「话 / 卷」扩展（按需加 `chapter_progress` 表）。
- 标签统计汇总仪表盘页。
- UI 打磨、中文文案、打包（electron-builder NSIS）、自动更新。
- 网络层：UNDICI 在 Electron 30(Node20) 有 `markAsUncloneable` 崩溃，已改用 `https-proxy-agent` 自写 `safeFetch` 并覆盖 `globalThis.fetch`；TMDB key / VNDB token 明文存 settings（后续可复用 vault 加密）。

---
*本文档为初步设计，后续可在实现中迭代细化（尤其是漫画"话/卷"与游戏购买信息的字段）。*
